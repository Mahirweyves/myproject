<li class="nav-item dropdown btn-primary mt-4 has text-dark">
        <strong class="nav-link dropdown-toggle has text-white" href="#" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Search by category</strong>
        <div class="dropdown-menu has text-center" aria-labelledby="dropdown01">
    
       <div class="mr-md-12">
       <div class="card">
       <div class="card-header">
          Categories
        </div>
        <ul class="list-group has text-left bg-dark">

        <% @categories.each do |category| %>

         <li>
          <%= link_to category.category, trades_path(:cate => category.id), class: "has text-white" %>

         <span class="badge badge-primary badge-pill has text-dark bg-warning"><%= category.trades.count %></span>
         </li>  
        <% end %>
        </ul></li>
        </div>
        </div></div></div>



<h1 class="page_title mb-3 mt-3 has text-center">Drop your message below:</h1>
<h3 class="page_title has text-center">Do you have any questions or feedback? We would love to hear from you!</h3>


 <div class="row pb-4">
 	<div class="ml-5 mt-5 mt-5">
 	- Call on:<a class="has text-warning"> 0788561729</a><br>
 	- Whatsapp:<a class="has text-warning"> 0788561729</a></div>
    <h2 class="text-center ml-5 pl-5">Contact Us:</h2>
    <div class="col-md-3 col-md-offset-4"> 

    <div class="card mt-4"><p class="page_title has text-dark">Please, fill in the form below and we will get back to you as soon as possible</p><div class="bg-warning">

      <%= flash[:notice] %>
<div class="card-body">
<div class="skinny_wrapper wrapper-padding">
   <%= form_for @contact do |f| %>

   <div class="has text-dark">
   <%= f.label :name %><br>
   <%= f.text_field :name, required: true, class: 'form-control has text-dark' %>

   <br>
   
   <%= f.label :email %><br>
   <%= f.text_field :email, required: true, class: 'form-control' %>

   <br>

   <%= f.label :message %><br>
   <%= f.text_area :message, as: :text, class: 'form-control' %>

    </div></div>

    <div class="text-center">
    <%= f.submit 'Send Message', class: 'btn btn-dark mt-3 mb-3' %>
    <%= link_to "Back", trades_path, class:" btn btn-primary mt-3 ml-5 mb-3" %>
    <% end %></div>
  </div>
</div></div></div></div>